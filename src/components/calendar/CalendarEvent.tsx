import React from 'react'
import { Event } from 'react-big-calendar'

export const CalendarEvent = (event: Event) => {
    console.log(event);
    
    return (
        <div>
            <p>s</p>
        </div>
    )
}
